#include <stdio.h>

int main() {
    int a = 10, b = 0;
    
    if (a != 0 && b != 0) {
        printf("a and b are non zero.");
    }
    else {
        printf("Either a or b or both are zero");
    }
}